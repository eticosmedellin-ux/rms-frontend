[build]
  command = "npm run build"
  publish = "dist"

# Sin esto, recargar la página en /inventario, /pos, etc. da 404 —
# Netlify necesita saber que React Router maneja esas rutas, no el servidor.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
